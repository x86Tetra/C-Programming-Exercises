#include <stdio.h>

// Note: It appears that if we don't declare any datatype of our main function,
// it'll default to the datatype of an integer; alongside a warning of implicit return types.
main()
{
	printf("Hello, World!\n");
}
